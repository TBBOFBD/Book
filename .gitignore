

# These should be ignored  as they are not needed for the project to run,
# but are needed for the project to be developed on. and to re
/Cargo.lock
/*.cargo
/main.rs
*.FIX

# These are the files that are generated by the build process
*.toml
target